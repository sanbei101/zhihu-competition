# vinext app

This project was created with create-vinext-app.

## Scripts

- `pnpm run dev` starts the vinext dev server.
- `pnpm run build` builds the production output (Node standalone).
- `pnpm run start` starts the production server with `vinext start` (default port 3000, or `PORT`).
